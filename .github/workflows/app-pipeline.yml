name: LearningSteps Deploy

on:
  push:
    branches: [ "main" ]

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Azure Login
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Show Azure Account
      run: az account show

    - name: Login to ACR
      run: az acr login --name acrwesteu

    - name: Build Docker image
      run: |
        docker build -t acrwesteu.azurecr.io/learningsteps-api:${{ github.sha }} ./app
        docker tag acrwesteu.azurecr.io/learningsteps-api:${{ github.sha }} acrwesteu.azurecr.io/learningsteps-api:latest

    - name: Push Docker image
      run: |
        docker push acrwesteu.azurecr.io/learningsteps-api:${{ github.sha }}
        docker push acrwesteu.azurecr.io/learningsteps-api:latest

    - name: Set AKS context
      uses: azure/aks-set-context@v3
      with:
        resource-group: rg-dev-aks
        cluster-name: aks-west-eu

    - name: Deploy to AKS
      run: |
        kubectl set image deployment/learningsteps-api \
        learningsteps-api=acrwesteu.azurecr.io/learningsteps-api:${{ github.sha }}

    - name: Verify rollout
      run: |
        kubectl rollout status deployment/learningsteps-api
        kubectl get pods -o wide
